import { ExerciseByWorkout } from './exercise-by-workout.entity';

describe('ExerciseByWorkout', () => {
  it('should be defined', () => {
    expect(new ExerciseByWorkout()).toBeDefined();
  });
});
