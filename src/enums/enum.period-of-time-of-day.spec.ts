import { EnumPeriodOfTimeOfDay } from './enum.period-of-time-of-day';

describe('EnumPeriodOfTimeOfDay', () => {
  it('should be defined', () => {
    expect(new EnumPeriodOfTimeOfDay()).toBeDefined();
  });
});
